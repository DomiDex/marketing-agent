---
description: Add, fix, or optimize schema markup and structured data (JSON-LD)
argument-hint: [schema type, e.g., "add FAQ schema to help center pages"]
---

## Schema Markup Command

### Instructions

1. **Invoke the schema-markup skill** to guide this interaction
2. **Gather context** about pages and schema requirements
3. **Follow the skill's methodology** for structured data
4. **Save all outputs** to: `output/schema-markup/`

### Output Requirements

**Directory**: `output/schema-markup/`
**Filename**: `schema_{descriptor}_{YYYY-MM-DD}.md`

Example: `schema_faq-pages_2026-01-22.md`

### User Input

$ARGUMENTS
